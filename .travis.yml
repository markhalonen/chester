language: go
sudo: false
go:
  - tip
before_install:
  - go get github.com/mattn/goveralls
jobs:
  include:
    - script: $GOPATH/bin/snapper test
    - script: $GOPATH/bin/goveralls -service=travis-ci
